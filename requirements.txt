flask>=3.0

# Headless Chromium. MCI-UVF results are rendered client-side, so this is the
# only way to read them -- see gflights.py. After installing, also run:
#   python -m playwright install chromium
playwright

# Fallback source for routes Google serves as server-rendered HTML.
fast-flights==3.1.0

# Undeclared dependency of fast-flights 3.1.0: it imports typing_extensions
# but does not list it, so a clean environment fails at import without this.
typing_extensions

# Optional: QR code in the local server banner, and --chart.
qrcode
matplotlib
